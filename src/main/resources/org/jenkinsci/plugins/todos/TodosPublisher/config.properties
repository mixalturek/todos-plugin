description.pattern=<a href="{0}">Fileset 'includes'</a> \
             		setting that specifies the generated XML TODOs report files, such as '**/todos.xml'. \
             		Basedir of the fileset is <a href="ws/">the workspace root</a>. \
             		If no value is set, then the default '**/todos.xml' is used. Be sure not to include any \
    		 		non-report files into this pattern. The report files must have been generated by TODOs \
                    using at least the "--out-xml todos.xml" option.
